#!/bin/sh
# Identical to running: python3 bot.py
# bot.py handles installing dependencies, ffmpeg/aria2, and starting the
# aria2c daemon itself - this script just makes that easy to invoke.
python3 bot.py
