{
  "name": "leech-encode-bot",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ffmpeg aria2 && pip install -r requirements.txt"
}
